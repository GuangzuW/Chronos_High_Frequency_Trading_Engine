version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Enum value names like ORDER_SIDE_BUY intentionally encode wire mappings.
    - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
    - FILE
